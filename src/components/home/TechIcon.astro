---
interface Props {
  id: string;
  class?: string;
}

const { id, class: className = 'home-tech-card__logo' } = Astro.props;
---

<img
  src={`/icons/tech/${id}.svg`}
  alt=''
  class={className}
  width='32'
  height='32'
  loading='lazy'
  decoding='async'
/>
