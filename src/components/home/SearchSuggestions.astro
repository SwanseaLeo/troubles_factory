---
interface Suggestion {
  query: string;
  label: string;
}

interface Props {
  suggestions: Suggestion[];
  align?: 'center' | 'start';
  class?: string;
}

const { suggestions, align = 'center', class: className } = Astro.props;
---

{
  suggestions.length > 0 && (
    <div class:list={['mt-5 w-full', className]}>
      <p
        class:list={[
          'tf-text-caption mb-2',
          align === 'center' ? 'text-center' : 'text-left',
        ]}
      >
        Try searching for
      </p>
      <ul
        class:list={[
          'flex flex-wrap gap-2',
          align === 'center' ? 'justify-center' : 'justify-start',
        ]}
      >
        {suggestions.map((item) => (
          <li>
            <a
              href={`/search?q=${encodeURIComponent(item.query)}`}
              class='tf-tag tf-tag--neutral tf-tag--pill tf-focus-ring'
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </div>
  )
}
